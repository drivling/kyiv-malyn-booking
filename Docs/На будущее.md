### 2.5 Проверить работу с датами 

**P1 · ~1ч · backend**

`toDateKey` использует `toISOString`. Для киевского времени дата, сохранённая как локальная полночь, превращается в предыдущий день. Влияет на группировку античита по дням и на дату в тексте поста для Facebook.

- [ ] Проверить, как реально пишется `ViberListing.date` и `RideCompletionProof.rideDate`
- [ ] При подтверждении проблемы — перейти на явную работу в таймзоне Europe/Kyiv
- [ ] Тест на дату около полуночи


### 3.2 Привести тексты в соответствие с правилами

**P1 · ~1ч · тексты**

Экран приглашения обещает «друг має зареєструватися в боті та додати попутку», по коду этого недостаточно — нужны два фото и апрув модератора. Приведший друга будет ждать бонус и не понимать, почему его нет.

- [ ] Переписать `startReferralInviteFlow` (`telegram-referral.ts:102-105`)
- [ ] Сверить формулировки промо, приглашения, `/help` и `Docs/referral-program-terms.md`