# 🚗 Viber Parser Service

Окремий сервіс для парсингу оголошень про поїздки з експортованих Viber чатів.

## 🎯 Що робить

1. **Приймає експортовані повідомлення** з Viber групи
2. **Парсить інформацію**:
   - Маршрут (Київ-Малин, Малин-Київ тощо)
   - Дата і час відправлення
   - Кількість вільних місць
   - Ціна
   - Контактний телефон
   - Ім'я відправника
3. **Зберігає в окрему таблицю** `ViberRide`
4. **Надає API** для отримання розпарсених даних

## 📋 Налаштування

### 1. Налаштування .env

```bash
cp .env.example .env
```

Заповніть змінні:

```env
DATABASE_URL="postgresql://user:password@host:5432/database"
PORT=3001
NODE_ENV=development
MAX_MESSAGE_AGE_DAYS=7
```

### 2. Локальне тестування

```bash
# Встановлення залежностей
npm install

# Створення таблиць в БД
npm run prisma:migrate

# Запуск в dev режимі
npm run dev
```

### 3. Експорт чату з Viber

**На Android:**
1. Відкрийте групу в Viber
2. Натисніть на назву групи (вгорі)
3. Прокрутіть вниз → "Експортувати чат"
4. Оберіть "Без медіа"
5. Відправте файл собі (email/telegram)

**На iOS:**
1. Відкрийте групу в Viber
2. Натисніть на назву групи
3. "Експортувати історію чату"
4. Оберіть "Без медіа"
5. Відправте файл

**На Desktop:**
1. Відкрийте групу
2. Меню → "Експортувати чат"
3. Збережіть текстовий файл

### 4. Імпорт повідомлень

Після експорту ви отримаєте текстовий файл з повідомленнями у форматі:

```
[ 9 лютого 2026 р. 12:55 ] ⁨Іван Петренко⁩: Київ-Малин завтра о 8:00, є 3 місця, 0501234567
[ 9 лютого 2026 р. 13:10 ] ⁨Марія Іванова⁩: Малин-Київ 10.02 о 18:00, 2 пасажири, тел 0672345678
```

Відправте цей текст на сервер:

## 🚀 Деплой на Railway

### Створення окремого сервісу

1. **Додайте новий сервіс з цієї папки:**
   ```bash
   # В Railway dashboard: New → Deploy from GitHub repo
   # Root Directory: viberparser
   ```

2. **Додайте змінні середовища:**
   - `DATABASE_URL` - з існуючої PostgreSQL бази
   - `VIBER_BOT_TOKEN`
   - `VIBER_WEBHOOK_URL` - Railway згенерує домен
   - `PORT` - Railway встановить автоматично

3. **Після деплою:**
   - Скопіюйте URL сервісу (наприклад: `https://viberparser-production.up.railway.app`)
   - Оновіть `VIBER_WEBHOOK_URL` в змінних середовища
   - Redeploy сервіс

## 📊 API Endpoints

### GET /health
Перевірка стану сервісу

```bash
curl https://your-domain.railway.app/health
```

### GET /api/rides
Отримати розпарсені поїздки

```bash
curl https://your-domain.railway.app/api/rides?active=true&limit=20
```

Параметри:
- `active` - тільки активні (default: true)
- `parsed` - тільки розпарсені (default: true)
- `limit` - максимальна кількість (default: 50)

### GET /api/stats
Статистика парсера

```bash
curl https://your-domain.railway.app/api/stats
```

Відповідь:
```json
{
  "totalMessages": 150,
  "parsedMessages": 120,
  "activeRides": 45,
  "parsingRate": "80.00%",
  "lastCheck": "2026-01-28T10:30:00Z",
  "messagesProcessed": 150,
  "errors": 5
}
```

### POST /api/import
Імпорт експортованого чату

```bash
# З файлу
curl -X POST https://your-domain.railway.app/api/import \
  -H "Content-Type: text/plain" \
  --data-binary @viber_export.txt

# Або прямо текст
curl -X POST https://your-domain.railway.app/api/import \
  -H "Content-Type: application/json" \
  -d '{"messages": "[ 9 лютого 2026 р. 12:55 ] ⁨Ім'\''я⁩: текст"}'
```

Відповідь:
```json
{
  "success": true,
  "imported": 45,
  "skipped": 5,
  "errors": 0,
  "total": 50
}
```

### POST /api/deactivate-old
Деактивувати старі оголошення

```bash
curl -X POST https://your-domain.railway.app/api/deactivate-old \
  -H "Content-Type: application/json" \
  -d '{"days": 7}'
```

## 🔧 Структура проекту

```
viberparser/
├── src/
│   ├── index.ts          # Головний файл, Viber bot, API
│   └── parser.ts         # Логіка парсингу повідомлень
├── prisma/
│   └── schema.prisma     # Схема БД (ViberRide, ViberParserState)
├── package.json
├── tsconfig.json
├── railway.json
├── .env.example
└── README.md
```

## 📝 База даних

### Таблиця ViberRide

Зберігає кожне повідомлення з групи:
- Сирі дані (текст, відправник, час)
- Розпарсені дані (маршрут, дата, час, місця, ціна, телефон)
- Метадані (чи розпарсено, чи активне, помилки)

### Таблиця ViberParserState

Зберігає стан парсера:
- Останнє оброблене повідомлення
- Кількість оброблених повідомлень
- Кількість помилок

## 🧪 Приклади повідомлень для парсингу

Парсер розпізнає такі формати (з експортованого чату):

```
[ 9 лютого 2026 р. 12:55 ] ⁨Іван⁩: Київ-Малин завтра о 8, є 3 місця, 0501234567
[ 9 лютого 2026 р. 13:10 ] ⁨Марія⁩: Малин-Київ 10.02 о 18:00, 2 пасажири, 0672345678
[ 10 лютого 2026 р. 08:30 ] ⁨Петро⁩: Київ Малин сьогодні 08:30, 4 місця, тел: +380501234567
[ 10 лютого 2026 р. 14:20 ] ⁨Олена⁩: Малин Київ 11.02 о 18, 2 особи, 0631234567
```

## ⚠️ Обмеження та особливості

1. **Формат експорту:**
   - Працює з експортованими чатами Viber
   - Формат: `[ дата ] ⁨Ім'я⁩: повідомлення`
   - Потрібно експортувати БЕЗ медіа файлів

2. **Якість парсингу:**
   - Залежить від формату повідомлень
   - Можуть бути хибні спрацювання
   - Рекомендується перевіряти результати

3. **Правові аспекти:**
   - Переконайтеся, що маєте право використовувати ці дані
   - Експортуйте тільки з груп, де це дозволено
   - Дотримуйтесь правил конфіденційності
   - Номери телефонів зберігаються тільки для зв'язку

## 🔗 Інтеграція з основним сайтом

Для показу Viber оголошень на сайті:

1. **Додайте ендпоінт в основному backend:**

```typescript
app.get('/api/viber-rides', async (req, res) => {
  const response = await fetch('https://viberparser.railway.app/api/rides');
  const rides = await response.json();
  res.json(rides);
});
```

2. **Використайте в frontend:**

```typescript
const viberRides = await fetch('/api/viber-rides').then(r => r.json());
```

## 🔄 Автоматизація

Для автоматичного оновлення можна налаштувати:

1. **Cron job** для періодичного експорту і імпорту
2. **Скрипт** який експортує чат і відправляє на сервер
3. **Railway Cron Jobs** для очищення старих оголошень

Приклад скрипту для автоматичного імпорту:

```bash
#!/bin/bash
# import-viber.sh

EXPORT_FILE="viber_export.txt"
API_URL="https://your-domain.railway.app/api/import"

# Імпортуємо
curl -X POST "$API_URL" \
  -H "Content-Type: text/plain" \
  --data-binary @"$EXPORT_FILE"

echo "Import completed!"
```

## 📚 Додаткова інформація

- [Viber Export Guide](https://support.viber.com/hc/articles/360002219314)
- [Railway Deployment Guide](https://docs.railway.app/)
- [Prisma Documentation](https://www.prisma.io/docs/)
