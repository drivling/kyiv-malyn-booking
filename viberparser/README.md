# 🚗 Viber Parser Service

Окремий сервіс для парсингу оголошень про поїздки з Viber груп.

## 🎯 Що робить

1. **Підключається до Viber** через Bot API
2. **Читає повідомлення** з групи (бот повинен бути доданий до групи)
3. **Парсить інформацію**:
   - Маршрут (Київ-Малин, Малин-Київ тощо)
   - Дата і час відправлення
   - Кількість вільних місць
   - Ціна
   - Контактний телефон
4. **Зберігає в окрему таблицю** `ViberRide`
5. **Надає API** для отримання розпарсених даних

## 📋 Налаштування

### 1. Створення Viber бота

1. Відкрийте Viber на комп'ютері
2. Перейдіть: Ще → Публічні акаунти → Створити бот
3. Або використайте: https://partners.viber.com/
4. Отримайте токен бота (Auth Token)

### 2. Налаштування .env

```bash
cp .env.example .env
```

Заповніть змінні:

```env
DATABASE_URL="postgresql://user:password@host:5432/database"
VIBER_BOT_TOKEN="ваш-токен-від-viber"
VIBER_BOT_NAME="RideParserBot"
VIBER_WEBHOOK_URL="https://your-domain.railway.app/viber/webhook"
PORT=3001
```

### 3. Локальне тестування

```bash
# Встановлення залежностей
npm install

# Створення таблиць в БД
npm run prisma:migrate

# Запуск в dev режимі
npm run dev
```

**Для локального тестування webhook:**

1. Встановіть ngrok: `npm install -g ngrok`
2. Запустіть: `ngrok http 3001`
3. Скопіюйте HTTPS URL (наприклад: `https://abc123.ngrok.io`)
4. Встановіть webhook вручну:

```bash
curl -X POST https://chatapi.viber.com/pa/set_webhook \
  -H "X-Viber-Auth-Token: YOUR_BOT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"url":"https://abc123.ngrok.io/viber/webhook"}'
```

### 4. Додавання бота до групи

1. Створіть публічну групу в Viber
2. Знайдіть вашого бота через пошук
3. Додайте його до групи
4. Бот почне отримувати повідомлення

## 🚀 Деплой на Railway

### Створення окремого сервісу

1. **Додайте новий сервіс з цієї папки:**
   ```bash
   # В Railway dashboard: New → Deploy from GitHub repo
   # Root Directory: viberparser
   ```

2. **Додайте змінні середовища:**
   - `DATABASE_URL` - з існуючої PostgreSQL бази
   - `VIBER_BOT_TOKEN`
   - `VIBER_WEBHOOK_URL` - Railway згенерує домен
   - `PORT` - Railway встановить автоматично

3. **Після деплою:**
   - Скопіюйте URL сервісу (наприклад: `https://viberparser-production.up.railway.app`)
   - Оновіть `VIBER_WEBHOOK_URL` в змінних середовища
   - Redeploy сервіс

## 📊 API Endpoints

### GET /health
Перевірка стану сервісу

```bash
curl https://your-domain.railway.app/health
```

### GET /api/rides
Отримати розпарсені поїздки

```bash
curl https://your-domain.railway.app/api/rides?active=true&limit=20
```

Параметри:
- `active` - тільки активні (default: true)
- `parsed` - тільки розпарсені (default: true)
- `limit` - максимальна кількість (default: 50)

### GET /api/stats
Статистика парсера

```bash
curl https://your-domain.railway.app/api/stats
```

Відповідь:
```json
{
  "totalMessages": 150,
  "parsedMessages": 120,
  "activeRides": 45,
  "parsingRate": "80.00%",
  "lastCheck": "2026-01-28T10:30:00Z",
  "messagesProcessed": 150,
  "errors": 5
}
```

## 🔧 Структура проекту

```
viberparser/
├── src/
│   ├── index.ts          # Головний файл, Viber bot, API
│   └── parser.ts         # Логіка парсингу повідомлень
├── prisma/
│   └── schema.prisma     # Схема БД (ViberRide, ViberParserState)
├── package.json
├── tsconfig.json
├── railway.json
├── .env.example
└── README.md
```

## 📝 База даних

### Таблиця ViberRide

Зберігає кожне повідомлення з групи:
- Сирі дані (текст, відправник, час)
- Розпарсені дані (маршрут, дата, час, місця, ціна, телефон)
- Метадані (чи розпарсено, чи активне, помилки)

### Таблиця ViberParserState

Зберігає стан парсера:
- Останнє оброблене повідомлення
- Кількість оброблених повідомлень
- Кількість помилок

## 🧪 Приклади повідомлень для парсингу

Парсер розпізнає такі формати:

```
Київ-Малин завтра о 8, є 3 місця, 150грн. Тел: 0501234567
Малин -> Київ 29.01 о 18:00, 2 місця, тел 380501234567
K-M сьогодні 08:30, місць 4, ціна 100 грн
Malyn-Kyiv 30/01 9:00, available 2 seats
```

## ⚠️ Обмеження

1. **Viber Bot API обмеження:**
   - Бот повинен бути доданий до групи
   - Не може читати історію (тільки нові повідомлення)
   - Потребує публічного webhook URL

2. **Якість парсингу:**
   - Залежить від формату повідомлень
   - Можуть бути хибні спрацювання
   - Рекомендується перевіряти результати

3. **Правові аспекти:**
   - Переконайтеся, що маєте право збирати ці дані
   - Додавайте бота тільки до груп, де це дозволено
   - Дотримуйтесь правил конфіденційності

## 🔗 Інтеграція з основним сайтом

Для показу Viber оголошень на сайті:

1. **Додайте ендпоінт в основному backend:**

```typescript
app.get('/api/viber-rides', async (req, res) => {
  const response = await fetch('https://viberparser.railway.app/api/rides');
  const rides = await response.json();
  res.json(rides);
});
```

2. **Використайте в frontend:**

```typescript
const viberRides = await fetch('/api/viber-rides').then(r => r.json());
```

## 📚 Додаткова інформація

- [Viber Bot API Documentation](https://developers.viber.com/docs/api/rest-bot-api/)
- [viber-bot NPM package](https://www.npmjs.com/package/viber-bot)
- [Railway Deployment Guide](https://docs.railway.app/)
