import { test } from '@playwright/test';

/**
 * Placeholder so `npm run test:e2e` exits 0 until real smoke specs (iter 2).
 * Remove this file when the first real e2e spec is added.
 */
test.describe.skip('e2e skeleton (iter 2)', () => {
  test('placeholder', async () => {
    // intentional skip
  });
});
