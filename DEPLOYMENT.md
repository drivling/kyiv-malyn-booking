# Варіанти деплою проекту

## 📊 Порівняння варіантів

### Варіант 1: Railway (Рекомендовано) ⭐

**Що включає:**
- Frontend (React)
- Backend (Node.js)
- PostgreSQL база даних
- Все в одному місці

**Ціни:**
- **Hobby план:** $5/місяць (500 годин виконання)
- **Pay-as-you-go:** ~$8-15/місяць для малого трафіку
- Безкоштовний кредит $5 на старті
- Без додаткових плат за bandwidth

**Переваги:**
- ✅ Найкращий developer experience
- ✅ Автоматичний деплой з GitHub
- ✅ One-click PostgreSQL
- ✅ Простий setup
- ✅ Детальні логи
- ✅ Автоматичні SSL сертифікати

**Недоліки:**
- ⚠️ Pay-as-you-go може бути непередбачуваним при високому трафіку

**Орієнтовна вартість:** $8-15/місяць

---

### Варіант 2: Render

**Що включає:**
- Frontend (React)
- Backend (Node.js)
- PostgreSQL база даних
- Все в одному місці

**Ціни:**
- **Free tier:** Безкоштовно (з обмеженнями, спить після неактивності)
- **Starter план:** $7/місяць (frontend) + $7/місяць (backend) + $7/місяць (DB) = **$21/місяць**
- **Standard план:** $25/місяць (frontend) + $25/місяць (backend) + $20/місяць (DB) = **$70/місяць**

**Переваги:**
- ✅ Безкоштовний tier для тестування
- ✅ Надійність (zero downtime)
- ✅ Cloudflare CDN включено
- ✅ Автоматичні SSL
- ✅ Простий інтерфейс

**Недоліки:**
- ⚠️ Free tier спить після неактивності (не підходить для продакшену)
- ⚠️ Дорожче за Railway для production

**Орієнтовна вартість:** $21-70/місяць

---

### Варіант 3: Vercel (Frontend) + Railway/Render (Backend + DB)

**Що включає:**
- Frontend на Vercel (React)
- Backend + DB на Railway або Render

**Ціни:**
- **Vercel:** Безкоштовно (Hobby) або $20/місяць (Pro)
- **Railway (Backend + DB):** $8-15/місяць
- **Разом:** $8-35/місяць

**Переваги:**
- ✅ Vercel - найкращий для React/Vite
- ✅ Швидкий CDN
- ✅ Оптимальна продуктивність для frontend
- ✅ Автоматичний деплой

**Недоліки:**
- ⚠️ Два сервіси для управління
- ⚠️ Vercel Pro платний для production

**Орієнтовна вартість:** $8-35/місяць

---

### Варіант 4: Fly.io

**Що включає:**
- Frontend (React)
- Backend (Node.js)
- PostgreSQL база даних
- Все в одному місці

**Ціни:**
- **Free tier:** 3 shared VMs, 160GB bandwidth
- **Pay-as-you-go:** ~$15-31/місяць для малого трафіку
- Безкоштовний кредит на старті

**Переваги:**
- ✅ Безкоштовний tier з хорошими обмеженнями
- ✅ Global edge deployment (30+ регіонів)
- ✅ Швидкий для глобального трафіку
- ✅ Full VM control

**Недоліки:**
- ⚠️ Складніший setup ніж Railway
- ⚠️ Менш популярний (менше документації)

**Орієнтовна вартість:** $15-31/місяць

---

### Варіант 5: DigitalOcean App Platform

**Що включає:**
- Frontend (React)
- Backend (Node.js)
- PostgreSQL база даних (окремо)
- Все в одному місці

**Ціни:**
- **Basic план:** $5/місяць (frontend) + $5/місяць (backend) + $15/місяць (DB) = **$25/місяць**
- **Professional:** $12/місяць (frontend) + $12/місяць (backend) + $15/місяць (DB) = **$39/місяць**

**Переваги:**
- ✅ Передбачувані ціни
- ✅ Надійність
- ✅ Хороша документація
- ✅ 45-65% економія vs Vercel

**Недоліки:**
- ⚠️ Дорожче за Railway
- ⚠️ Менш автоматизований

**Орієнтовна вартість:** $25-39/місяць

---

### Варіант 6: Supabase (DB) + Vercel (Frontend) + Railway (Backend)

**Що включає:**
- Frontend на Vercel
- Backend на Railway
- PostgreSQL на Supabase

**Ціни:**
- **Supabase Free:** Безкоштовно (500MB DB, 2GB bandwidth)
- **Supabase Pro:** $25/місяць (8GB DB, 50GB bandwidth)
- **Vercel:** Безкоштовно (Hobby)
- **Railway:** $8-15/місяць
- **Разом:** $8-40/місяць

**Переваги:**
- ✅ Supabase має безкоштовний tier
- ✅ Додаткові можливості Supabase (Auth, Storage)
- ✅ Гнучкість

**Недоліки:**
- ⚠️ Три сервіси для управління
- ⚠️ Складніший setup

**Орієнтовна вартість:** $8-40/місяць

---

## 🎯 Рекомендації

### Для початку (малий трафік):
**Railway** - найкращий баланс ціни, простоти та функціональності ($8-15/місяць)

### Для економії:
**Fly.io** - безкоштовний tier може вистачити на початку ($0-15/місяць)

### Для максимальної продуктивності:
**Vercel (Frontend) + Railway (Backend + DB)** - оптимальна швидкість ($8-35/місяць)

### Для передбачуваних витрат:
**DigitalOcean** - фіксовані ціни ($25-39/місяць)

---

## 📋 Що потрібно для деплою

1. **GitHub репозиторій** (для автодеплою)
2. **Змінні оточення:**
   - `DATABASE_URL` - для Prisma
   - `ADMIN_PASSWORD` - пароль адміна
   - `VITE_API_URL` - URL бекенду (для frontend)
3. **Build скрипти** (вже є в package.json)
4. **Prisma міграції** (потрібно запустити при деплої)

---

## 🚀 Наступні кроки

Після вибору варіанту я створю:
1. Конфігураційні файли для деплою
2. Скрипти для автодеплою
3. Детальну інструкцію в README
4. Налаштування змінних оточення
5. Налаштування Prisma міграцій

---

## 🌐 Два домени на одному фронтенді (malin.kiev.ua + korosten.kiev.ua)

Обидва домені веде **один** Railway-сервіс `frontend` — окремого деплою чи білду не потрібно.

**Railway:** сервіс `frontend` → Settings → Domains → додати `korosten.kiev.ua`
(за бажанням `www.korosten.kiev.ua`) і прописати видані CNAME у реєстратора.
Бекенд, БД, Telegram-бот і `VITE_API_URL` не змінюються.

**Що робить код** (`frontend/scripts/site-hosts.mjs` — мапа доменів, `frontend/src/site/*` — фронт):

| Поведінка | Де реалізовано |
|---|---|
| Зміна «Рідне місто» на Коростень → перехід на `korosten.kiev.ua` з тим самим шляхом і query (місто їде в `?city=`, бо куки між доменами не ходять) | `siteConfig.buildCitySwitchUrl` + `MizhgorodskiPage` |
| Домен пінить рідне місто (на `korosten.kiev.ua` це завжди Коростень); вибір іншого міста повертає на `malin.kiev.ua` | `MizhgorodskiPage`, `useHomeCityHandoff` |
| `/admin`, `/login`, `/user` → 301 на `malin.kiev.ua` (віджет Telegram Login прив'язаний до одного домену бота) | `serve-dist.mjs` (сервер) + `DomainGuard` (SPA-навігація) |
| `korosten.kiev.ua` поки не індексується: `X-Robots-Tag: noindex`, `robots.txt` = `Disallow: /`, `sitemap.xml` = 404; canonical усіх сторінок ведуть на `malin.kiev.ua` | `serve-dist.mjs` |
| Локальний транспорт показує заглушку «скоро» в містах без розкладу | галочка `TripPoint.hasLocalTransport` в адмінці → `LocalTransportGate` |

**Локальна перевірка:** `korosten.localhost:5173` (резолвиться в 127.0.0.1) або `?site=korosten`.

**Коли з'явиться контент під Коростень** — прибрати noindex у `serve-dist.mjs`
і зробити для нього власні canonical/sitemap.
